# Contributing (HutchGroup LLC site)

This is a static HTML deployment.

## Local preview
Open `hutchgroup_production_website.html` in your browser.

## Deployment
This repo is deployed to Vercel.

- No build step
- Output directory: `.`

## Changes
- Keep changes minimal and readable.
- Avoid adding heavy frameworks unless explicitly requested.
- Respect `prefers-reduced-motion`.
